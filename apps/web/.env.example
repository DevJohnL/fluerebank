# Em desenvolvimento: deixe vazio para o mesmo host do Vite e o proxy encaminhar /api → http://127.0.0.1:3000 (vite.config.ts).
# Alternativa explícita (a API tem de estar a correr; CORS já está ativo): VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL=
